import styles from "./TableFrame.module.css";
import TopItem from "../../models/TopItem";

interface TableFrameProps {
    items: TopItem[];
    title: string;
}

const TableFrame = ({items, title}: TableFrameProps) => {
    return (
        <div></div>
    );
}
